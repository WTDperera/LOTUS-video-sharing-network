# ==============================================
# LOTUS VIDEO PLATFORM - DOCKER COMPOSE
# Production-ready multi-container deployment
# ==============================================

version: '3.8'

services:
  server:
    build:
      context: ./server
    container_name: lotus-server
    ports:
      - "5000:5000"
    networks:
      - lotus-net
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://wtdperera2001:s.RhdiPmRi52n5!@lotus.jhbgi0u.mongodb.net/lotus_video?retryWrites=true&w=majority
      - JWT_SECRET=tharindu_super_secret_key_2026_secured_version
      - NODE_ENV=production
    restart: unless-stopped

  client:
    build:
      context: ./client
      args:
        - VITE_API_URL=http://localhost:5000/api
    container_name: lotus-client
    ports:
      - "3000:80"
    networks:
      - lotus-net
    depends_on:
      - server
    restart: unless-stopped

networks:
  lotus-net:
    driver: bridge

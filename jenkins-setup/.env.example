# Jenkins Environment Variables (Optional)
# Copy to .env and customize if needed

# Jenkins Java Options
# JAVA_OPTS=-Xmx2048m -Xms512m -Djava.awt.headless=true

# Jenkins Home Directory (already mapped via volume)
# JENKINS_HOME=/var/jenkins_home

# Jenkins HTTP Port (already set to 8080)
# JENKINS_PORT=8080

# Jenkins Agent Port (already set to 50000)
# JENKINS_AGENT_PORT=50000

# Skip Initial Setup Wizard (set to false for manual setup)
# JENKINS_SKIP_SETUP=false

# Docker Hub Credentials (for pushing images)
# DOCKER_USERNAME=your_dockerhub_username
# DOCKER_PASSWORD=your_dockerhub_password

# GitHub Personal Access Token (for private repos)
# GITHUB_TOKEN=your_github_token
